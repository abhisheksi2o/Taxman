[project]
name = "astra-tax-backend"
version = "0.1.0"
description = "ASTRA Tax – AI income tax filing & reconciliation agent (backend)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = ["ignore::DeprecationWarning"]

[tool.ruff]
line-length = 110
target-version = "py311"
